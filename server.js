const express = require('express')

const app = express();

//**Settings **//
let timeout = 5;
let allow_deleting = true; //Default [true]
let allow_notifications = true; //Default = [true]
let allow_popups = true; //Defautl = [true]
app.listen(PORT, () => {
    console.log
});